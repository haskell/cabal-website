name: prettier

on:
  push:
  pull_request:

jobs:
  prettier:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: prettier
        uses: creyD/prettier_action@v4.3
        with:
          dry: true
